Generate JavaScript function lexClojure from the pseudocode below.
Do not use keywords "class", "new", and "this".

Format:
window.foo = function(arg1, arg2) {
    ... body
}

Implement "is whitespace" and "is digit" using the charcodes. Do not use regex.

Report errors in the spec.